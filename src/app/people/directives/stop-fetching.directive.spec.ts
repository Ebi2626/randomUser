import { StopFetchingDirective } from './stop-fetching.directive';

describe('StopFetchingDirective', () => {
  it('should create an instance', () => {
    const directive = new StopFetchingDirective();
    expect(directive).toBeTruthy();
  });
});
